# Downloaded Mermaid artifacts (entry + transitive chunks), produced by
# MermaidDownloader. Not source — regenerate with the downloader, or mirror
# by hand (see README.md). Kept out of version control.
mirror/
